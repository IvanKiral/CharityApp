package com.kiral.charityapp.network.Dto

/*class ProfilePostMapper: Mapper<ProfilePostDto, Profile> {
    override fun mapToDomainModel(model: ProfilePostDto): Profile {
        return Profile(
            id = 0,
            name = "",
            email = "",
            donations = 0,
            credit = 0.0,
            region = "",
            categories = model.categories,
            regularDonationActive = model.regularDonationActive ?: false,
            regularDonationFrequency = model.regularDonationFrequency ?: 0,
            regularDonationValue = model.regularDonationValue ?: 0.0,
            badges = listOf()
        )
    }

    override fun mapFromDomainModel(domainModel: Profile): ProfilePostDto {
        return ProfilePostDto(
            userId = domainModel.id,
            regularDonationFrequency =  domainModel.regularDonationFrequency,

        )
    }
}*/